name: flutter_omise_payments
description: Omise js wrapper for flutter | Android | IOS | Web
version: 1.0.2
homepage: https://github.com/necessarylion/flutter_omise_payments

environment:
  sdk: '>=2.18.1 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^3.0.4
  http: ^0.13.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
